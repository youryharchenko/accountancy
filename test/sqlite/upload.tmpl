{
    "request": {
        "command": "system",
        "service": "upload"
    },
    "db": {
        "driver": "sqlite3",
        "connection": "./test/sqlite/test.db",
        "show": true
    },
    "body": {
        "uuid": "{{.UUID}}",
        "name": "{{.Name}}",
        "props": {},
        "content": "{{.Content}}"
    }
}